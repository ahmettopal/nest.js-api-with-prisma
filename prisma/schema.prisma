generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model User {
    id       BigInt  @id(map: "PK_User") @default(autoincrement())
    name     String
    username String
    biyo     String?
}
